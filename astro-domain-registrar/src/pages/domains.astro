---
import Layout from '../layouts/Layout.astro';
import DomainSearch from '../components/react/DomainSearch.jsx';

// Get initial search query from URL parameters, if any
const initialQueryFromUrl = Astro.url.searchParams.get('query');
---

<Layout title="Search Domains - Domain Registrar">
  <h1>Search for Available Domains</h1>
  <p>Find the perfect domain name for your next project.</p>

  <DomainSearch initialQuery={initialQueryFromUrl} client:load />

</Layout>
```
